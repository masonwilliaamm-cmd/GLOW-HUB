# Decisions Log

Tracks the founder decisions flagged as open in `docs/build-plan.md`, so later phases don't re-ask.

| Decision | Status | Choice | Notes |
|---|---|---|---|
| Payment provider | **Confirmed** | Paystack | Wired for real in Phase 5 (pay-in + Transfers API for payouts). |
| Auth method | **Confirmed** | Email + password | Not phone-based OTP. Implemented in Phase 2. |
| App hosting | **Confirmed** | Railway | Switched from Vercel — see README "Deployment" section. Railway also hosts Postgres and Redis (via plugins), replacing the separate Neon/Upstash setup below. Unlike Vercel, Railway is a persistent Node process (via Nixpacks: `npm install` → `npm run build` → `npm start`), not per-request serverless — this actually suits the app's current eager-connect Postgres/Redis clients better. Per-PR preview links (the original reason for choosing a host at all) work differently on Railway than Vercel's zero-config default — confirm "PR Environments" is enabled in Railway's project settings; not verified from this repo. |
| `sharp` package required for self-hosting | **Confirmed** | Added as a dependency | `next/image` optimization does a hard `require('sharp')` at runtime with no fallback on a self-hosted Node server (confirmed by reading Next.js's own image-optimizer source) — Vercel didn't need it because Vercel's infrastructure intercepts image optimization before that code path runs. Without this, every `<Image>` on the storefront (`ProductCard`, product detail page) would 500 on Railway. |
| Managed Postgres / Redis provider | **Superseded** | Railway's own Postgres + Redis plugins | Was Neon + Upstash (kept below for history — that setup is no longer live). Real Railway connection strings are expected in chat/`.env.local`, gitignored, never committed — ask the founder to resend if a fresh session needs them. **Use the plugins' public/TCP-proxy connection strings when running migrate/seed from outside Railway** — the `*.railway.internal` hostnames only resolve inside Railway's own network, not from this sandbox or the founder's machine. |
| ~~Neon + Upstash (superseded)~~ | ~~Confirmed~~ | ~~Neon (Postgres) + Upstash (Redis)~~ | Kept for history. Neon's pooled connection string sidestepped Supabase's earlier IPv6 direct-connection issue; migration/seed against it could not be run from this Claude Code sandbox (network policy blocks outbound traffic outside a small allowlist — confirmed via raw TCP tests to both Neon port 5432 and Upstash port 6379, and even proxied HTTPS to Upstash). If reviving this setup: this Prisma version (7.10.0)'s `prisma.config.ts` only supports `url`/`shadowDatabaseUrl` in `datasource` — no `directUrl` despite some docs describing one — so a pooled connection string needs manually swapping to the unpooled one just for `migrate`/`seed` if Prisma's advisory locks fail through PgBouncer. |
| Cart model (single vs multi-vendor) | Open | — | Build plan recommends one order per vendor at checkout; confirm before Phase 4. |
| Escrow release percentage on dispatch | Open | — | Build plan suggests 80–90%; confirm before Phase 5. |
| Delivery confirmation timeout window | Open | — | Build plan suggests 5–7 days; confirm before Phase 5. |
